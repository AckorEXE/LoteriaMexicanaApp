import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pedro.loteriamexicana',
  appName: 'Loteria Mexicana',
  webDir: 'dist'
};

export default config;
